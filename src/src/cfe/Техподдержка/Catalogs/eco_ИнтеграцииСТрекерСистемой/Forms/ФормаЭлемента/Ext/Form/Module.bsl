
&НаСервереБезКонтекста
Процедура ТестСоединенияНаСервере(Результат)
	// Вставить содержимое обработчика.
КонецПроцедуры

&НаКлиенте
Процедура ТестСоединения(Команда)
	Результат = Ложь;
	ТестСоединенияНаСервере(Результат);
	Если Результат Тогда
		СтрокаСообщения = "ru = 'Тест прошел успешно!'";
	Иначе
		СтрокаСообщения = "ru = 'Тест провален!'";
	КонецЕсли;
	ПоказатьПредупреждение(,СтрокаСообщения ,,"ru = 'Проверка соединения с трекер системой'");
КонецПроцедуры
