
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Результат = Ложь;
	eco_ИнтеграцияRedmine.ТестСоединенияНаСервере(Результат);
	Если Результат Тогда
		СтрокаСообщения = НСтр("ru = 'Тест прошел успешно!'");
	Иначе
		СтрокаСообщения = НСтр("ru = 'Тест провален!'");
	КонецЕсли;
	ПоказатьПредупреждение(,СтрокаСообщения ,,НСтр("ru = 'Проверка соединения с трекер системой'"));
	
КонецПроцедуры
